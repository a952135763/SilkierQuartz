{{!<Layout}}
{{ViewBag Title='登录'}}

<div class="ui inverted page dimmer" id="dimmer"><div class="ui loader"></div></div>
<form class="ui form" method="post" enctype="multipart/form-data">
    <div class="ui clearing basic segment" style="padding: 0px" id="header">
        <h1 class="ui left floated header">
            授权登录
        </h1>
    </div>
    {{#with Model}}
    <div class="ui segment" style="width: 700px; height: 250px;">
        <div>
            <div class="field accept-error">
                <label>Name</label>
                <input type="text" placeholder="用户名" value="{{UserName}}" name="userName" />
            </div>
            <div class="field accept-error">
                <label>Password</label>
                <input type="password" placeholder="密码" value="{{Password}}" name="password" />
            </div>

            <div class="field accept-error">
                <div class="ui checkbox">                    
                    <input type="checkbox" value="true" {{#if IsPersist}} checked {{/if}} name="isPersist" id="isPersist" />
                    <input name="isPersist" type="hidden" value="false">
                    <label for="isPersist">记住我</label>
                </div>
            </div>
        </div>
        <div style="float: left; margin-top: 15px;">
            <div class="ui buttons">
                <button type="submit" class="ui primary button">Login</button>
            </div>
        </div>
    </div>
    {{#if IsLoginError}}
    <div class="ui negative message">
        <p>用户名或密码不正确!</p>
    </div>
    {{/if}}
    {{/with}}
</form>

<script src="Content/Scripts/post-validation.js"></script>
